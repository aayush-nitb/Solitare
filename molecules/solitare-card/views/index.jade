extends layout

block css
    style.
        #card{
            width: 73px;
            height: 98px;
            background-image: url('#{url("images/card-back-blue.png")}');
            background-position: 0 0;
            background-size: 100% 100%;
        }

block js
    script.
        function images(file) {
            return "url(#{url('images/')}" + file + ")"
        }
    script(src="#{url('controllers/script.coffee')}")

block molecules

block content
    #card